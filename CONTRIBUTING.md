# Contributing

Use an issue to establish scope before substantial work. Every change must keep
`npm run check` green, preserve protocol boundaries, and avoid logging or
persisting credentials unexpectedly.
